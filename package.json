{
  "name": "basa_project",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "tailwindcss -i ./src/source.css -o ./src/style.css && vite",
    "build": "tailwindcss -i ./src/source.css -o ./src/style.css && tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "tailwindcss": "^3.3.5",
    "typescript": "^5.0.2"
  },
  "dependencies": {
    "vite": "^5.0.2"
  }
}
